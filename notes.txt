Notes:


ssh into oracle vm:
ssh ubuntu@170.9.225.9
scp -i id_ed25519 ubuntu@170.9.225.9

build go server
production: go build -o server_app.exe
debug: go build -tags debug -o server_app_debug.exe





2 do:
- mqtt protocol define, create documentation. implement on both device and server

- handle periodic heartbeat from/to device? (extra precaution in case LWT or dev_bootup messages insufficient)
	devices check in every X min. server marks heartbeat list of devices active. timer every X min, check if any active devices marked inactive in heartbeat list.

- test:
	devices file
	weather file
	dev_bootup -> active, LWT -> inactive
	debug build

2 do Connected Device:
- @bootup: device publish t=dev_bootup, m=dev_name, zip
- implement last will testament for mqtt client at setup?



Commit summary:

